import React from 'react';
import { spotify } from '..';

const TopArtists = () => {
     //spotify().getMe().then(user => console.log)
  return (
    <div>TopArtists</div>
  )
}

export default TopArtists;